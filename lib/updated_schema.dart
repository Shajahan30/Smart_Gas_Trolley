import 'package:flutter/material.dart';

class updatedschema extends StatefulWidget {
  const updatedschema({Key? key}) : super(key: key);

  @override
  State<updatedschema> createState() => _updatedschemaState();
}

class _updatedschemaState extends State<updatedschema> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
